<script lang="ts">
	import { getDayNames } from './utils.ts';

	let { firstDayOfWeek }: { firstDayOfWeek: 0 | 1 } = $props();

	const dayNames = $derived(getDayNames(firstDayOfWeek));
</script>

<div class="grid grid-cols-7 border-t border-x border-b border-frame select-none">
	{#each dayNames as name}
		<div class="py-2 text-center text-[11px] font-medium text-muted-contrast uppercase tracking-wide">
			{name}
		</div>
	{/each}
</div>

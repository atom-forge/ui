<script lang="ts">
	import type { DiagramData } from '../../diagram-editor/types.ts';

	type DiagramBlockData = DiagramData & { caption?: string; description?: string };

	let { data }: { data: DiagramBlockData | null } = $props();
</script>

<figure class="flex flex-col items-center gap-2">
	{#if data?.svg}
		<div class="overflow-x-auto rounded-lg border border-frame p-3 w-full flex justify-center" style="--diagram-scheme: {data.scheme ?? 'light'}">
			{@html data.svg}
		</div>
	{:else}
		<div class="rounded-lg border border-dashed border-frame p-6 w-full text-center text-xs text-muted-contrast italic">
			No diagram
		</div>
	{/if}

	{#if data?.caption?.trim()}
		<figcaption class="text-sm font-medium text-canvas-contrast text-center">{data.caption}</figcaption>
	{/if}

	{#if data?.description?.trim()}
		<p class="text-xs text-muted-contrast text-center leading-relaxed max-w-prose">{data.description}</p>
	{/if}
</figure>
